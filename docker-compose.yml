version: '3.8'
services:
  formflow-app:
    build: .
    ports:
      - "8080:5000"
    env_file:
      - .env
    volumes:
      - ./pdf_output:/app/pdfs:Z
      - ./forms:/app/forms:Z
      - ./pdf_templates:/app/pdf_templates:Z
      - ./static:/app/static:Z
    restart: unless-stopped
